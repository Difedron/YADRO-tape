# YADRO Tape Sort

YADRO Tape Sort - это тестовое задание в команду Tatlin.Unified DataPath.

Проект реализует сортировку целых чисел на устройстве хранения данных типа **Tape** — лента.

Основная идея проекта — отсортировать данные с входной ленты и записать результат на выходную ленту, учитывая ограничение по оперативной памяти и особенности ленточного устройства.

---

## Описание задания

В условии задано устройство хранения данных типа **лента**.

Лента работает следующим образом:

- считывающая/записывающая головка находится над текущей ячейкой;
- чтение и запись возможны только в текущую ячейку;
- лента может двигаться влево и вправо;
- перемещение ленты считается затратной по времени операцией;
- устройство не предназначено для произвольного доступа к данным.

Есть:

- входная лента длины `N`, содержащая элементы типа `integer`;
- выходная лента такой же длины;
- ограничение оперативной памяти — не более `M` байт;
- возможность использовать разумное количество временных лент.

Необходимо записать в выходную ленту элементы с входной ленты, отсортированные по возрастанию.

---

## Основная идея решения

Так как входная лента может быть очень большой, а оперативная память ограничена, нельзя просто считать все числа в `std::vector`, отсортировать их и записать обратно.

Поэтому в проекте используется алгоритм **External Merge Sort**( то есть внешняя сортировка слиянием).

Этот алгоритм хорошо подходит для случаев, когда данные не помещаются целиком в оперативную память.

---

## Почему выбран External Merge Sort?

Обычная сортировка, например:

```cpp
std::sort(data.begin(), data.end());
```

подразумевает, что все данные уже находятся в оперативной памяти.

Но по условию память ограничена параметром `M`, поэтому такой подход не подходит для больших входных данных.

В свою очередь EMS решает эту проблему:

1. данные читаются небольшими блоками.
2. каждый блок помещается в память и сортируется.
3. отсортированные блоки записываются во временные файлы.
4. затем временные блоки соединяются в один общий отсортированный результат.

Такой подход не требует загружать всю входную ленту в память одновременно.

---

## Как работает алгоритм?

Алгоритм состоит из двух основных этапов.

### 1. Создание отсортированных временных блоков

Сначала входная лента читается блоками.

Размер блока определяется ограничением памяти:

```text
max_numbers = memory_limit_bytes / sizeof(int)
```

Например, если:

```text
memory_limit_bytes = 12
sizeof(int) = 4
```

то за один раз можно прочитать только 3 числа.

Данный параметр меняется в файле config.txt, там же меняются остальные параметры задержки ленты.

Каждый блок:

1. читается с входной ленты;
2. сохраняется во временный `std::vector<int>`;
3. сортируется с помощью `std::sort`;
4. записывается во временную ленту в директории `tmp`.

Пример:

```text
input: 8 3 1 7 4 2 9 5
```

При `memory_limit_bytes = 12` получаются блоки:

```text
8 3 1   -> 1 3 8
7 4 2   -> 2 4 7
9 5     -> 5 9
```

В директории `tmp` создаются временные файлы:

```text
tmp/run_0.txt
tmp/run_1.txt
tmp/run_2.txt
```

---

### 2. Слияние временных блоков

После создания временных отсортированных лент они сливаются в один общий отсортированный поток.

Слияние выполняется попарно и многопроходно. За один проход алгоритм открывает только две временные ленты и создаёт новую временную ленту с результатом их слияния. Это позволяет не держать в памяти элемент от каждой временной ленты сразу и не нарушать ограничение `memory_limit_bytes` при большом количестве блоков.

Для каждой пары временных лент алгоритм:

1. считывает текущие значения с двух лент;
2. записывает меньшее значение в новую временную ленту;
3. сдвигает только ту ленту, из которой было взято значение;
4. повторяет процесс, пока обе временные ленты не закончатся.

Итоговый результат:

```text
1 2 3 4 5 7 8 9
```

---

## Структура проекта

```text
YADRO-tape/
├── include/
│   ├── ITape.h
│   ├── FileTape.h
│   ├── TapeConfig.h
│   └── TapeSorter.h
├── src/
│   ├── main.cpp
│   ├── FileTape.cpp
│   ├── TapeConfig.cpp
│   └── TapeSorter.cpp
├── tests/
│   └── tape_tests.cpp
├── tmp/
│   └── .gitkeep
├── CMakeLists.txt
├── config.txt
├── input.txt
├── .gitignore
└── README.md
```

---

## Описание основных компонентов

### `ITape`

Файл:

```text
include/ITape.h
```

Это интерфейс для работы с устройством типа лента.

Он описывает базовые операции, которые должна поддерживать любая реализация ленты:

```cpp
virtual int read() = 0;
virtual void write(int value) = 0;

virtual void moveLeft() = 0;
virtual void moveRight() = 0;
virtual void rewind() = 0;

virtual bool isEnd() const = 0;
```

Этот интерфейс нужен, чтобы отделить саму идею ленты от конкретного способа её реализации.

---

### `FileTape`

Файлы:

```text
include/FileTape.h
src/FileTape.cpp
```

`FileTape` — это класс, который реализует интерфейс `ITape` и эмулирует работу ленты через обычный файл.

Внутри класса хранится:

- имя файла;
- текущая позиция головки;
- размер ленты;
- настройки задержек;
- файловый поток.

Каждая ячейка ленты хранит одно целое число. При открытии существующего файла `FileTape` потоково нормализует его в формат строковых ячеек фиксированного размера: 12 символов под число и перенос строки.

```text
           1
           8
           7
          12
```

Входные числа до нормализации могут быть разделены любыми пробельными символами. После нормализации чтение и запись текущей ячейки выполняются через прямой переход к позиции `position * 13`, поэтому чтение, запись и смещение головки выполняются за O(1).

---

### `TapeConfig`

Файлы:

```text
include/TapeConfig.h
src/TapeConfig.cpp
```

`TapeConfig` отвечает за чтение конфигурации из файла `config.txt`.

Через конфиг задаются:

- задержка чтения;
- задержка записи;
- задержка сдвига;
- задержка перемотки;
- ограничение оперативной памяти.

Это позволяет менять параметры работы программы без перекомпиляции.

---

### `TapeSorter`

Файлы:

```text
include/TapeSorter.h
src/TapeSorter.cpp
```

`TapeSorter` реализует алгоритм сортировки.

Основные методы:

- `createSortedRuns(...)` — создаёт временные отсортированные блоки;
- `mergeRuns(...)` — сливает временные блоки в выходную ленту;
- `sort(...)` — общий метод, который выполняет сортировку целиком.

---

### `main.cpp`

Файл:

```text
src/main.cpp
```

Основная программа:

1. принимает имена входного и выходного файлов из аргументов командной строки;
2. читает настройки из конфигурационного файла;
3. определяет количество чисел во входном файле;
4. открывает входной файл как файловую ленту;
5. запускает сортировку;
6. записывает результат в выходной файл.

---

## Конфигурационный файл

Файл:

```text
config.txt
```

Пример содержимого:

```text
read_delay_ms=0
write_delay_ms=0
move_delay_ms=0
rewind_delay_ms=0
memory_limit_bytes=12
```

Описание параметров:

| Параметр | Значение |
|---|---|
| `read_delay_ms` | задержка чтения одного элемента с ленты |
| `write_delay_ms` | задержка записи одного элемента на ленту |
| `move_delay_ms` | задержка сдвига ленты на одну позицию |
| `rewind_delay_ms` | задержка перемотки ленты в начало |
| `memory_limit_bytes` | ограничение оперативной памяти в байтах |

Все параметры можно менять без перекомпиляции проекта.

Например, если нужно увеличить доступную память:

```text
memory_limit_bytes=4096
```

Если нужно включить задержки:

```text
read_delay_ms=10
write_delay_ms=10
move_delay_ms=5
rewind_delay_ms=50
memory_limit_bytes=1024
```

---

## Формат входного файла

Входной файл воспринимается как лента и открывается через `FileTape`. Числа могут быть разделены пробелами, табами или переносами строк.

Пример:

```text
15 -3 8 8 0 42 -10 7 1 15
```

Также допустим вариант:

```text
15
-3
8
8
0
42
-10
7
1
15
```

---

## Формат выходного файла

Выходной файл содержит отсортированные числа.

Для входа:

```text
15 -3 8 8 0 42 -10 7 1 15
```

результат будет:

```text
         -10
          -3
           0
           1
           7
           8
           8
          15
          15
          42
```

Выходная и временные ленты записываются в формате «одно число на строку» с фиксированной шириной строки.

---

## Временные файлы

Во время работы программа создаёт временные файлы в директории временных лент. По умолчанию это `tmp`.

Пример:

```text
tmp/run_0.txt
tmp/run_1.txt
tmp/run_2.txt
tmp/merge_0.txt
```

Назначение файлов:

| Файл | Назначение |
|---|---|
| `run_*.txt` | временные отсортированные блоки |
| `merge_*.txt` | промежуточные результаты попарного слияния |

При каждом новом запуске старые временные ленты `run_*.txt` и `merge_*.txt` удаляются из директории временных лент.

---

## Сборка проекта

Проект собирается через CMake.

Из корня проекта:

```bash
mkdir build
cd build
cmake ..
cmake --build .
```

После сборки будут доступны два исполняемых файла:

```text
YADRO_tape
YADRO_tape_tests
```

---

## Запуск программы

Программа принимает два обязательных аргумента и два дополнительных:

```bash
./YADRO_tape <input_file> <output_file> [config_file] [tmp_directory]
```

Если дополнительные аргументы не переданы, используются значения по умолчанию:

```text
config_file = ../config.txt
tmp_directory = ../tmp
```

Пример запуска из директории `build`:

```bash
./YADRO_tape ../input.txt ../output.txt
```

Пример запуска с явным конфигом и директорией временных лент:

```bash
./YADRO_tape ../input.txt ../output.txt ../config.txt ../tmp
```

Пример вывода:

```text
Sorting completed successfully
Input file: ../input.txt
Output file: ../output.txt
Config file: ../config.txt
Temporary tapes directory: ../tmp
Numbers count: 10
```

После выполнения отсортированные данные будут записаны в файл:

```text
output.txt
```

---

## Тесты

В проекте используются юнит-тесты на основе **Catch2**.

Тесты находятся в файле:

```text
tests/tape_tests.cpp
```

Проверяются основные части проекта:

1. `TapeConfig` — корректная загрузка настроек из файла;
2. `FileTape` — чтение, запись и движение по файловой ленте;
3. `TapeSorter` — корректная сортировка данных через временные ленты;
4. создание директории временных лент;
5. соблюдение ограничения памяти при создании run-блоков и при слиянии.

---

## Запуск тестов

После сборки проекта из директории `build`:

```bash
./YADRO_tape_tests
```

Ожидаемый результат:

```text
All tests passed
```

---

## Подключение Catch2

Catch2 подключается через CMake с помощью `FetchContent`.

Это значит, что при первой сборке CMake автоматически загрузит Catch2 из GitHub.

Фрагмент из `CMakeLists.txt`:

```cmake
include(FetchContent)

FetchContent_Declare(
        Catch2
        GIT_REPOSITORY https://github.com/catchorg/Catch2.git
        GIT_TAG v3.8.1
)

FetchContent_MakeAvailable(Catch2)
```

Если при первой сборке возникнет ошибка загрузки Catch2, нужно проверить интернет-соединение или доступ к GitHub.

---

## Пример полного запуска

### 1. Входной файл

`input.txt`:

```text
15 -3 8 8 0 42 -10 7 1 15
```

### 2. Конфигурация

`config.txt`:

```text
read_delay_ms=0
write_delay_ms=0
move_delay_ms=0
rewind_delay_ms=0
memory_limit_bytes=12
```

### 3. Сборка

```bash
mkdir build
cd build
cmake ..
cmake --build .
```

### 4. Запуск

```bash
./YADRO_tape ../input.txt ../output.txt
```

### 5. Результат

`output.txt`:

```text
         -10
          -3
           0
           1
           7
           8
           8
          15
          15
          42
```

---

## Используемые технологии

- C++17
- CMake
- Catch2
- STL:
    - `std::vector`
    - `std::sort`
    - `std::fstream`
    - `std::ifstream`
    - `std::ofstream`
    - `std::filesystem`


